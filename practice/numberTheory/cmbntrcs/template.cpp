#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define FIO ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);

void solve() {
	int n;
	cin >> n;

	int a[n];
	for (int i = 0; i < n; i++)
		cin >> a[i];

	return ;
}

int main() {
	FIO;

	int tc(1);
	cin >> tc;

	while (tc--) {solve();}

	return 0;
}
