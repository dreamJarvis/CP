#include <bits/stdc++.h>
using namespace std;

#define int long long

void solve() {
	int n;
	scanf("%lld", &n);

	int a[n];
	for (int i = 0; i < n; i++) {
		scanf("%lld", &a[i]);
	}

	return ;
}

signed main() {
	FIO;

	int tc;
	scanf("%lld", &tc);

	while (tc--) solve();

	return 0;
}